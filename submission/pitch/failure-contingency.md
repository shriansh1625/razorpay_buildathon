# Failure contingency

| Failure | Recovery |
|---|---|
| Wrong seed / empty world | Do not click Run Recovery on camera. Restart `revive control-room` (seed 14 default). |
| Wow opportunity missing | Palette → restore demo seed 14 (if shown). Or restart the process. |
| Benchmark “not mounted” | Say: evidence is a frozen tree mounted at `artefacts/benchmark/official-cloud-final/`. Show declared 20×6×5 contract from Benchmark Lab even if cells are absent. Prefer a machine where the tree is present. |
| Matrix still indexing | Stay on Overview: 600 / 120 / seal. Do not wait on a spinner as the close. |
| Blocked path needed | Keep `opp_WST4PPPH81VPNTNC18K0YGRAW9` in a second tab. |
| Browser crash | Reload `#/control`. Session rebuilds seed 14 on server restart. |
| Someone asks about LLMs | “Diagnosis is deterministic in this build. Official benchmark is LLM_OFF. We did not fake a model.” |
| Someone asks for live money | “Synthetic sandbox. Official M-10 is the experiment, not this hero figure.” |
