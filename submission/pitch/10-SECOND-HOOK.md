# 10-second hook

PAYVANTA doesn't just retry failed payments.

It decides what recovery is worth doing, checks whether it is safe, executes inside bounds, and measures the incremental net result — with the engine evaluated across 600 official cells.
