# M13.16 — Parallel Official Configuration Propagation Fix

**Status:** COMPLETE  
**Official benchmark executed:** NO  
**Date:** 2026-08-24

---

## Root cause

`BenchmarkMode.OFFICIAL.value` is `"OFFICIAL"` but worker used `mode == "official"` → `default_draft_policy_pack()` while still calling `official_benchmark_config()`.

See `implementation/m13-16-parallel-config/root-cause.md`.

---

## Fix

| Change | Location |
|--------|----------|
| `policy_pack_to_frozen_payload()` | `config/policy_pack.py` |
| `policy_pack_from_frozen_payload()` fail-closed | `config/policy_pack.py` |
| Serialize parent PolicyPack in worker payload | `cells/parallel_worker.py` |
| Parent hash/version assertions | `cells/parallel.py` |
| Worker config_hash invariant check | `cells/parallel_worker.py` |

---

## Tests

`tests/benchmark/test_m13_16_parallel_config.py` — pickle roundtrip, OFFICIAL mode, reject DRAFT, hash mismatch, config invariants.

---

**STOP — await explicit review before official benchmark run.**
