# M13.19 — Preflight + Official Run 2

**Status:** PREFLIGHT PASSED — OFFICIAL RUN 2 IN PROGRESS

## Fix applied (2026-08-25)

First preflight attempt blocked by freeze gate treating `seed_set=(1,)` as incomplete.

- `check_freeze_prerequisites(..., preflight=True)` — validates sealed official experiment with exactly seed=1
- Runner passes `preflight=True` for `BenchmarkMode.PREFLIGHT`
- Test: `test_preflight_freeze_allows_single_seed`

## Preflight result (2026-08-25, ~3.5h)

- **30/30 cells** — `preflight_gate.json` → `passed: true`
- B0: `intervention_count=0` on all 6 profiles
- B1/B2/B3/REVIVE: `intervention_count > 0` on all 6 profiles
- REVIVE: contacts + net recovery measured (e.g. BALANCED net_recovered_paise=1,171,692,313)
- Manifest: `implementation_revision=m13.18-execution-bridge-v1`, `benchmark_runner_version=0.13.18-m13.18`
- Output: `artefacts/benchmark/preflight-m13-19` (PREFLIGHT_ONLY — NOT BENCHMARK EVIDENCE)

## Official Run 2 (in progress)

```powershell
revive benchmark --mode official --workers 2 --output artefacts/benchmark/official-run2
```

Invalidated Run 1 preserved at `artefacts/benchmark/official/`.
