# M13.20 Three-Worker Validation

**Status:** COMPLETE — **THREE-WORKER READY**

## Decision

| Check | Result |
|-------|--------|
| workers=1/2/3 aggregate fingerprint | PASS |
| workers=1/3 cell metrics_checksum | PASS |
| workers=3 resume (stop_after_cell=5) | PASS |
| workers=3 memory safe (~8 GB) | PASS (~1.3 GB estimated parallel peak) |

Matrix: 10 cells (seed=1, BALANCED + HIGH_NATURAL, frozen official scale).  
Report: `implementation/m13-20-three-worker-validation/validation-report.json`

## Fixes during M13.20

1. Live CLI progress (post-persistence group/cell lines)
2. `verify_group_persisted()` failure isolation
3. Worker peak RSS + 6 GB memory gate
4. `config_from_worker_payload()` honours `profile_set` for preflight/sealed development runs
5. Draft-pack development runs skip `frozen_experiment_reference_hash` in runner

## Next step (not executed here)

Official Run 2 resume with `--workers 3` is now validated for development/frozen representative work. Run official with workers=2 or 3 per operator choice — M13.20 did not run `--mode official --workers 3`.
