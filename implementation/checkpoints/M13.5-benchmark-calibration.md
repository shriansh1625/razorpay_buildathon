# M13.5 — Benchmark Freeze + Calibration Gate

**Status:** COMPLETE  
**Official benchmark run:** NOT EXECUTED (by design)  
**Date:** 2026-08-23  
**Tests:** 214 passing (3 new M13.5 calibration tests)

---

## Purpose

Determine whether the frozen benchmark can meaningfully distinguish REVIVE from documented baselines before official freeze — without running the official benchmark or making superiority claims.

---

## Diagnostic methodology

| Scale | Seeds | Profiles | Opportunities |
|-------|-------|----------|---------------|
| Calibration | 5 (1–5) | All 6 | 40 per cell |
| Tiny (M13 reference) | 1 | BALANCED | 12 |

Engine: `revive benchmark calibrate` → `implementation/m13-5-benchmark-calibration/`

---

## Results summary

| Gate | Classification | Status |
|------|----------------|--------|
| Environment realism | avg natural rate 0.61, 39 intervention-sensitive opps/cell | **READY** |
| Baseline separation | 30/30 cells clearly separated | **READY** |
| Scarcity | avg competition ratio 0.95; profile capacities not wired in M13 runner | **BLOCKED** |
| Action sensitivity | 3.7 recovering actions/opp, variance 0.18 | **READY** |
| Natural recovery | std 0.16, range [0.42, 0.97] | **READY** |
| B3/REVIVE differentiation | avg diff rate 2.7%, 6 weak cells | **CAUTION** |
| Benchmark integrity | oracle isolation, same-world clone | **READY** |
| Reproducibility | development fingerprints identical | **READY** |
| Policy / ADR freeze | all DRAFT/PENDING | **BLOCKED** |

---

## Baseline separation

**CLEARLY_SEPARATED** — B0–B3 show materially different mid-cycle behavior across all 30 seed/profile cells.

Example (seed 1, BALANCED): B0=0, B1=16, B2=37, B3=36 selected actions.

Profiles show **identical** baseline counts per seed — `capacity_scarcity_factor` does not affect baseline runner today.

---

## Scarcity

**LOW SCARCITY** at calibration mid-cycle snapshot (avg competition ratio 0.95).

Even with diagnostic mapping of `capacity_scarcity_factor` to `ResourceCapacities`:

- SCARCE profile does not produce higher competition than BALANCED in observed baseline behavior
- M13 official runner does **not** wire profile scarcity to capacities (implementation gap)

At tiny scale (12 opps): competition ratio retry = **0.08** — explains M13 zero M-10.

At calibration scale (40 opps): competition ratio retry = **0.50**.

---

## Action sensitivity

**HIGH** — oracle shows ~3.7 recovering actions per opportunity with meaningful per-action outcome variance.

---

## Natural recovery

**HEALTHY** — natural recovery rate varies across profiles (HIGH_NATURAL elevated vs others via `natural_recovery_multiplier`).

---

## Profile integrity

Profiles differ through documented oracle mechanisms (natural multiplier, degradation, adversarial flag) — verified in `profile-analysis.md`.

**Gap:** SCARCE `capacity_scarcity_factor=2.5` is documented but not applied to benchmark `ResourceCapacities`.

---

## Parameter sensitivity

- ε materially changes B3 selection (23 → 10 selected across sweep) — ADR-011 resolution matters
- Opportunity count strongly affects competition (0.08 at 12 opps → 0.64 at 60 opps)
- M13 zero M-10 **not** interpreted as REVIVE failure — driven by tiny scale + insufficient resource contention

---

## B3/REVIVE differentiation

**WEAK DISTINCTION** — allocation differences occur (e.g. 3 differing opps at calibration scale seed 1) but rarely (~2.7% avg diff rate).

Portfolio allocator thesis is exercisable but **not strongly** at current calibration snapshot.

---

## Benchmark integrity

- Oracle isolation static checks: **pass**
- Same dataset hash on regeneration: **pass**
- World clone preserves opportunity count: **pass**
- No official benchmark run in M13.5

---

## Freeze prerequisites

All ADR-011/012/013, PolicyPack SEALED, approver model, predictor strategy, seed set, generator config: **BLOCKED**

---

## Final decision

# NOT READY FOR OFFICIAL FREEZE

### Structural blockers (must resolve before official benchmark)

1. Wire `capacity_scarcity_factor` to benchmark `ResourceCapacities` per profile (or equivalent documented mechanism)
2. Increase resource competition to MODERATE/HIGH under official scale — verify at ADR-012 scale before freeze
3. Accept ADR-011, ADR-012, ADR-013
4. Seal PolicyPack, freeze predictor/approver/seed set
5. Strengthen B3/REVIVE differentiation evidence (CAUTION → READY) or document acceptable weak distinction with rationale

### What is ready

- Environment contains recoverable revenue
- Baselines are distinct
- Natural recovery and action sensitivity are informative
- Integrity and reproducibility hold

---

## Known limitations

- Calibration uses development scale (40 opps), not official 500
- Mid-cycle single snapshot for scarcity/B3 analysis
- No REVIVE superiority claims
- No official benchmark executed

---

## Next step

**STOP.** Resolve blockers through legitimate engineering (profile capacity wiring, ADR acceptance, official scale validation) then re-run M13.5 or proceed to official freeze review.

Do **not** run official benchmark until freeze gate passes.

---

## Artefacts

```
implementation/m13-5-benchmark-calibration/
├── environment-diagnostics.md
├── baseline-separation.md
├── scarcity-analysis.md
├── action-sensitivity.md
├── natural-recovery-analysis.md
├── profile-analysis.md
├── parameter-sensitivity.md
├── b3-vs-revive-diagnostics.md
├── freeze-readiness.md
├── M13.5-decision.md
└── calibration_report.json
```

Command: `revive calibrate --output implementation/m13-5-benchmark-calibration`
