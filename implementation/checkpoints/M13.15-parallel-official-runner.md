# M13.15 — Parallel Official Benchmark Execution

**Status:** COMPLETE  
**Decision:** PARALLEL READY FOR OFFICIAL RUN  
**Official benchmark executed:** NO  
**Date:** 2026-08-24  
**Tests:** 7 new M13.15 + existing suite

---

## Objective

Expose M13.14-validated parallel execution through `revive benchmark --workers N` without changing benchmark semantics.

---

## CLI

```
revive benchmark --mode {development,official} [--workers N]
```

Default: `--workers 1` (legacy sequential behavior unchanged)

---

## Implementation

| File | Role |
|------|------|
| `cells/plan.py` | `BenchmarkGroup`, `plan_benchmark_groups()` |
| `cells/parallel_worker.py` | `run_seed_profile_group()` process worker |
| `cells/parallel.py` | `run_cell_benchmark_parallel()`, `validate_workers()` |
| `cells/runner.py` | `workers` parameter, dispatches to parallel path |
| `runner.py` | `execute_benchmark(workers=...)` |
| `cli.py` | `--workers` flag |

Work unit: **(seed, profile)** — one world, five policies sequential.

---

## Validation (development only)

| Check | Result |
|-------|--------|
| workers=1 vs workers=2 fingerprints | **Match** |
| Aggregate fingerprint | **Match** |
| Parallel resume | **Match** uninterrupted |
| Speedup (dev 2-profile) | **1.76×** |
| Config hash worker guard | **Enforced** |

---

## Explicit confirmation

- [x] Frozen benchmark configuration unchanged
- [x] M4–M12 semantics unchanged
- [x] M8 / B3 / generator unchanged
- [x] Official 600-cell benchmark NOT executed
- [x] Default workers=1 preserves legacy behavior
- [x] Tests pass

---

**STOP — M13.15 complete. Await explicit review before official benchmark.**
