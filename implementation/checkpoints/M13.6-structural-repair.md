# M13.6 — Structural Benchmark Repair + Re-calibration

**Status:** COMPLETE  
**Official benchmark run:** NOT EXECUTED (by design)  
**Date:** 2026-08-23  
**Tests:** 219 passing (4 new profile-capacity tests)

---

## Root cause

M13.5 identified that `capacity_scarcity_factor` from generation profiles was documented but **not wired** into benchmark `ResourceCapacities` or baseline BF-4 environment constraints. All profiles used default capacities, so SCARCE/ABUNDANT did not affect resource availability and scarcity gates failed.

---

## Repair

Implemented `revive/benchmark/capacities.py`:

```text
profile → capacity_scarcity_factor → capacity = base / factor
```

Wired into:

- `revive/benchmark/official/policy_runner.py` (REVIVE + baselines)
- `revive/benchmark/official/baseline_pipeline.py` (BF-4 constraints)
- `revive/benchmark/calibration/scarcity.py`

| Profile | retry_slots | message_capacity | incentive_budget (paise) |
|---------|-------------|------------------|--------------------------|
| SCARCE | 20 | 40 | 400,000 |
| BALANCED | 50 | 100 | 1,000,000 |
| ABUNDANT | 250 | 500 | 5,000,000 |

Policy-neutral: capacities depend on profile only, never strategy identity.

---

## Tests

- `tests/benchmark/test_profile_capacities.py` — SCARCE vs ABUNDANT differentiation, digest stability, baseline env alignment, policy-neutrality
- Existing calibration and benchmark suite unchanged and passing

---

## Scarcity results (post-repair)

| Scale | Classification | avg competition ratio |
|-------|----------------|------------------------|
| 40-op calibration | MODERATE SCARCITY | ~2–3 |
| 500-op official proposal | **HIGH SCARCITY** | 7.77 |

Profile wiring verified: SCARCE cells show lower capacities and higher deferral than ABUNDANT at same seed.

---

## 500-opportunity results (development calibration)

**Not official benchmark.** Config: 500 opportunities, 100 customers, 30-day window, 3 seeds × 6 profiles.

| Gate | Result |
|------|--------|
| Environment realism | READY |
| Baseline separation (official scale) | CLEARLY_SEPARATED — SCARCE/ABUNDANT now differ in B2/B3 counts |
| Scarcity | HIGH SCARCITY |
| Action sensitivity | READY |
| Natural recovery | READY |
| B3/REVIVE differentiation | **COLLAPSED** (0% differing allocations) |
| Benchmark integrity | READY |
| Reproducibility | identical fingerprints |

Scale sensitivity (BALANCED, seed 1, 21-day window):

| Opportunities | competition_retry | B3/REVIVE differing |
|-----------------|-------------------|---------------------|
| 100 | 1.22 | 8 |
| 250 | 3.06 | 35 |
| 500 | 5.82 | 43 |
| 750 | 9.42 | 21 |

ADR-012 scale (500) is **not frozen** — sufficiency evaluated only.

---

## B3/REVIVE results

At documented official-scale config: **COLLAPSED** — identical action selections and total ENRV across all 18 cells (3 seeds × 6 profiles).

Observed behavior:

- B3 greedy fills retry capacity with highest-ENRV actions
- REVIVE selects the same actions; defers lower-value opportunities (e.g. 196 deferred at BALANCED) without swapping winners
- Shadow prices present (retry_slots ≈ 19,907 paise) but do not change the selected set vs B3

Scale-sensitivity config (different window/customer ratio) shows non-zero differentiation — collapse is **config-specific**, not a global allocator bug.

**No M8 objective changes were made** (per M13.6 scope).

---

## Parameter sensitivities

| Parameter | Finding |
|-----------|---------|
| ε (ADR-011) | Not material at calibration scale; ADR-011 remains DRAFT |
| PolicyPack | DRAFT — no silent PROVISIONAL→FROZEN |
| B1 schedule (ADR-013) | Reviewed — faithful baseline, DRAFT |
| Approver model | simulated_v1_provisional — deterministic, not frozen |

---

## Remaining blockers

1. **B3/REVIVE differentiation collapsed** at official-scale config — benchmark cannot measure portfolio thesis
2. ADR-011 (ε) not ACCEPTED
3. ADR-012 (official scale) not ACCEPTED
4. ADR-013 (B1 schedule) DRAFT
5. PolicyPack not SEALED
6. Predictor / approver / official seed set not frozen

---

## Freeze recommendation

# NOT READY FOR FREEZE PREPARATION

Scarcity repair succeeded; environment now exercises HIGH contention. Structural gap: at the proposed official scale, portfolio allocation produces **identical** decisions to B3 greedy ENRV. Further ADR decisions required before freeze preparation — not automatic tuning of M8 for differentiation.

---

## Limitations

- Official benchmark not run
- No REVIVE superiority claims
- Scale sensitivity uses alternate generator window (21 vs 30 days) — documented in reports
- ε sensitivity at tiny calibration scale only
- M13.6 does not authorize M14, UI, voice, or learning

---

## Artifacts

- `implementation/m13-6-structural-repair/` (8 reports + JSON)
- CLI: `revive repair-calibrate --output implementation/m13-6-structural-repair`

**STOP** — await explicit approval before official benchmark or M14.
