# M13.14 Decision

## PERFORMANCE READY FOR OFFICIAL RUN

M13.14 reduced seed=2 BALANCED REVIVE stress cell from **1058 s → 480 s (2.20×)** with **identical** `metrics_checksum` vs M13.13 feasibility reference. Peak RSS ~551 MB (safe). Projected 600-cell median runtime **~20 h** (down from M13.13 conservative **40 h**). Parallel 2-worker test (2 REVIVE cells): **2.81× wall speedup**, fingerprints match sequential. M8 reference allocation equivalence preserved (M13.12). All six profiles validated (15-cycle cache equivalence, seed=1).

Remaining hotspot if further optimization needed: **M6 + M7 per-opportunity loop volume** (see `hotspot-analysis.md`).

**Official benchmark NOT executed. STOP — await explicit review.**
