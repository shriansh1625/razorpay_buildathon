# M13.16 Decision

## PARALLEL CONFIG FIX COMPLETE

Official `--workers 2` workers now receive the exact sealed PolicyPack from the parent process. Root cause was `BenchmarkMode.OFFICIAL.value == "OFFICIAL"` vs worker check `mode == "official"`, causing DRAFT reconstruction. **Official 600-cell benchmark has NOT been run.**
