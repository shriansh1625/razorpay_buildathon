# System Performance

- Logical processors: from `os.cpu_count()`
- RAM: ~8 GB machine (user stated)
- Peak REVIVE RSS: ~552 MB (M13.14 class)
- No official run active during M13.21
- Run 4 marked `PARTIAL_NON_EVIDENCE` — do not resume

Use `python scripts/m13_21_forensic.py` for full system capture with psutil when available.
