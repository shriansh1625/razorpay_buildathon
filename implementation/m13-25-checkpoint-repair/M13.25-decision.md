# M13.25 Decision

## CHECKPOINT_RESUME_READY

Checkpoint/resume integrity repaired without changing benchmark semantics or touching production preflight artifacts.

### Root cause

Parallel runs persisted cells incrementally but updated the manifest only after full group completion; stale manifests were not reconciled against valid files at startup.

### Fix

Startup `reconcile_checkpoint` + parent-only `sync_checkpoint_from_persisted` after verified groups.

### Checklist

- [x] files-ahead-of-manifest repaired
- [x] manifest-ahead-of-files repaired
- [x] corrupt-cell rejection
- [x] interruption/resume
- [x] workers=1/2/3 fingerprint equality
- [x] final aggregate equality
- [x] no production artifact modification
- [x] M13.25 tests pass (9/9)

Official benchmark **not** executed. Old `artefacts/cloud-preflight-w8/` left untouched.
