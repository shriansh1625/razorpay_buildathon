# M13.19 — Corrected Benchmark Preflight

## Command

```powershell
revive benchmark --mode preflight --workers 2 --output artefacts/benchmark/preflight-m13-19
```

## Mode: `PREFLIGHT`

- Uses `preflight_benchmark_config()` → sealed `pol_m13_official_v1`, official scale
- Matrix: **seed=1 × 6 profiles × 5 policies = 30 cells**
- Label: `PREFLIGHT_ONLY — NOT BENCHMARK EVIDENCE`
- Output: **separate directory** (never overwrites `artefacts/benchmark/official/`)

## Hashing

| Field | Meaning |
|-------|---------|
| `config_hash` | This run's config (includes `seed_set: [1]`) |
| `frozen_experiment_reference_hash` | Stable experiment identity (excludes seed matrix) — matches full official frozen experiment |

## Manifest fields (Run 2 ready)

- `implementation_revision`: `m13.18-execution-bridge-v1`
- `benchmark_runner_version`: `0.13.18-m13.18`

## Gate (`preflight_gate.json`)

Pass criteria per profile (seed=1):

- **B0**: `intervention_count == 0`
- **B1/B2/B3/REVIVE**: `intervention_count > 0`

## After preflight passes

```powershell
revive benchmark --mode official --workers 2 --output artefacts/benchmark/official-run2
```

Invalidated Run 1 preserved at `artefacts/benchmark/official/`.
