# M13.15 Decision

## PARALLEL READY FOR OFFICIAL RUN

`--workers 2` is validated on development frozen-scale cells. Workers=1 vs workers=2 produce **identical** cell and aggregate fingerprints. Checkpoint resume works under parallel completion order. Memory remains safe on 8 GB (default `workers=1`). **Official 600-cell benchmark has NOT been run.**

After explicit review:

```bash
revive benchmark --mode official --workers 2 --output artefacts/benchmark/official
```
